FROM maven:3.9.9-eclipse-temurin-17

WORKDIR /app

# Warm the Maven dependency cache first so iterative rebuilds stay fast.
COPY pom.xml ./
RUN mvn -q -DskipTests dependency:go-offline

COPY src ./src

EXPOSE 8080

CMD ["mvn", "spring-boot:run"]
