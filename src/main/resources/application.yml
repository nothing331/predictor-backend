server:
  port: ${PORT:8080}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:file:.env[.properties]
---
# Development Profile (Docker)
spring:
  config:
    activate:
      on-profile: dev | demo

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      url: ${REDIS_URL}

  flyway:
    enabled: true
    schemas: market
    default-schema: market
    create-schemas: true

  jpa:
    hibernate:
      ddl-auto: validate # Only validate, don't auto-create (we use init scripts)
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: market

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
            redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:8080/auth/google/callback}

app:
  demo:
    auth:
      enabled: false
  jwt-secret: ${JWT_SECRET}
  jwt-expiry-ms: 900000 
  jwt-refresh-expiry-day: 30
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS}
  analytics:
    posthog:
      enabled: ${POSTHOG_ENABLED:false}
      api-key: ${POSTHOG_API_KEY:}
      host: ${POSTHOG_HOST}

ratelimit:
  window-seconds: 60
  trade:
    max: 30
  sse-connect:
    max: 20
  fail-closed: false

# Async settlement worker. Default-on; disable per-replica by setting enabled=false.
# See docs/adr/0002-async-settlement-via-postgres-queue.md.
settlement:
  worker:
    enabled: true
    poll-ms: 1000             # @Scheduled fixedDelay between batches
    batch-size: 50            # max rows processed per tick
    max-attempts: 5           # transient retries before terminal failure
    base-backoff-seconds: 30  # exponential base: 30s, 1m, 2m, 4m, 8m
    jitter-seconds: 10        # uniform [0, jitter] added to each backoff
